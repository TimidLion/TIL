# Case Class

Case Class doesn't need ***new*** for making object.

Case class compared as the structure, not reference.

```
case class Book(isbn: String)

val frankenstein = Book("978-0486282114")
```
